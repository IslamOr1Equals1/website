// opencti integration client — stub, not yet wired
// Activate by setting the corresponding env var and implementing fetch calls
// via apiClient from lib/api/client.ts

export const openctiClient = {
  // Methods will be added when the integration is activated
}
