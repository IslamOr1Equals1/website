// Canonical shared type for security impact case study cards.
// Used by both SecurityImpactSection (homepage) and ServicesResultsSection (services page).
export type { WorkCard, WorkType } from '@/services/security-impact.service'
